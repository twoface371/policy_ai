# 실제 설정 — 법제처 API 키, OpenAI 키, DB 비밀번호가 들어간다.
# 배포처마다 값이 다르므로 config.example.json만 추적한다.
config.json

# 파이썬
.venv/
__pycache__/
*.py[cod]

# 수집·생성 산출물 — 법제처 원본 스냅샷과 리포트는 재수집·재생성된다
raw/
reports/

# macOS
.DS_Store
